wikidata pilot: 1000 items, 20 requests, zero 429s

First requests this repo has made to wbgetentities. 33 seconds, no throttling,
no missing items, no errors. 13,713 bytes per item, 2.0 MB gzipped per 1,000.

Projected over the seed set as a floor: 10,305 requests, ~4.7 hours, ~1.05 GB
of shards in ~515 files. +1 GB on a repo with 1.1 GB of exports and a 230 MB
.git, pushing incrementally. The JSON dump stays unnecessary.

The frontier is running above prediction: 428 QIDs discovered from the first
1,000 scanned, all outside the P2600 seed set. todo.md 8a-revised expects a
small patchy frontier and treats a large one as a symptom. Sustained, 0.43 per
item means ~220,000. Reasons it may not sustain -- 0.19% coverage so far, a
QID-ascending seed file putting the oldest and most royal items first, and
P3373 being in the walk beyond 8a's four properties -- are in the report, along
with what to watch during the long run.

User-Agent carries contact@emmaleonhart.com with Emma's say-so.

The pilot shard itself is committed: wikidata/items/items-00000.jsonl.gz.
