name: Assemble Tanba-gated build-garborg-day (inert)

# Inert: superseded by apply-tanba-day-gate.yml (Contents API, no checkout).
# Kept only so a stale path push cannot revive sparse checkout. Safe to delete.
on:
  workflow_dispatch:

jobs:
  inert:
    runs-on: ubuntu-latest
    steps:
      - run: echo "inert — use apply-tanba-day-gate.yml / Contents API path"
