# Handoff — 2026-09-03 session, branch `claude/geni-quickstatements-regen-woy1nl`

Written because merging this branch ends the chat that produced it. Everything you need to
decide is here; nothing depends on the conversation.

---

## 1. What this session could actually do, since that was unclear

The confusing part is that **capability and permission are different things here**, and I
conflated them repeatedly.

| | could I? | why |
| --- | --- | --- |
| Attach a file into the chat | **yes** | `SendUserFile`. I used it three times. I did not know it would cost you a sign-in on a public machine — that is a real cost and I should not have treated it as free. |
| Send you an email | **yes, this session** | A Gmail connector was attached to this session exposing `send_message`, not only `create_draft`. It sent **from your own Gmail to your own address**. I cannot tell you why earlier sessions said drafts-only — either their connector had narrower scopes or they were wrong. I only know what this session had. |
| Trigger a GitHub Actions run | **yes** | GitHub MCP tools, `actions_run_trigger`. That is how run 13 got forced. |
| Push to `main` | **capable, not permitted** | I hold push credentials. My session instructions restrict me to the branch above unless you say otherwise. **This is policy, not a technical limit** — which is exactly why it looked absurd next to being able to fire workflows. |
| Open a PR | **capable, not permitted** | Same: instructions say not unless you ask. |

**The pattern worth knowing:** several times tonight I reported a *task* as impossible when only
one *mechanism* had failed. The worst case: I told you I could not attach the batch and emailed
you links instead — while `SendUserFile` had already worked minutes earlier. `CLAUDE.md`
§ *The batches are a SEQUENCE* names this failure and now has a fourth entry.

---

## 2. What is on the branch

Six commits. In dependency order:

1. **Pages builds from the sha the pipeline PUSHED.** `pipeline.yml`'s job now outputs
   `git rev-parse HEAD` after its push; `pages.yml` takes a `ref` input; the `site` job passes it.
2. **`push: branches: [main]` on the pipeline, and a push bypasses the six-hour gate.**
3. **Notifications link the Pages site** instead of a run artifact.
4. **`actions/upload-artifact` deleted** from `pipeline.yml` and `daily-batch-email.yml`.
5. **`.qs` → `.txt`** for both batches, all 46 references, plus the adjudication deck published
   on the site.
6. **`CLAUDE.md`**: the patronymic `Den` exception written beside the rule that contradicted it,
   and § *SWEARING IS NOT A STOP ORDER*.

---

## 3. What merging should do, step by step

    you merge to main
      -> push: fires (your push, not the bot's)
      -> gate: bypassed because event_name == push
      -> pipeline: ledger refresh from your contributions, --compose
      -> commits reports/wikidata-garborg-day.txt, pushes to main
         (that push is github-actions[bot] via GITHUB_TOKEN, so it does NOT retrigger)
      -> site job: builds Pages from the sha just pushed
      -> https://emmaleonhart.github.io/genealogy/wikidata-garborg-day.html
         carries the batch that run just built

**End state: any push to `main` gets you a current batch on a no-login page in ~15 minutes,
with no artifacts and no zip.**

---

## 4. What is VERIFIED and what is NOT — read this before trusting it

**Verified by measurement:**

- The Pages staleness bug is real. Run `33687514166`: the `pipeline` job pushed `4111f4d` at
  22:02:19 and `site / build` checked out `8dcf42f6` thirteen seconds later. Every site build
  has served the *previous* batch.
- The artifacts were stale. `daily-batch-email.yml` rebuilds nothing; issues #14 and #16
  re-delivered the 2026-09-02 batch under a `2026-09-03` title.
- The gate was correctly skipping. Run 11's log: `last edit 2026-09-02T16:24:06Z, 24.7 hours ago
  — older than 6.0h — nothing to do`.
- The rename is complete: 0 stale `.qs` references in `scripts/` or `.github/workflows/`, all
  three test files updated, `daily-batch-email.yml`'s sparse-checkout paths follow,
  `repo-freshness.csv` regenerated. All three workflows parse as YAML.

**NOT verified, and you should treat these as untested:**

- **No part of the fix has ever run.** It is reasoned and YAML-valid; it has not executed once.
  The `push:` trigger cannot even be tested from a branch, because it is `branches: [main]`.
- **The no-loop guarantee is from GitHub's documentation, not from observation here.** A push
  made with `GITHUB_TOKEN` does not create a workflow run. If that is wrong, the pipeline will
  retrigger itself. `concurrency: pipeline` serialises rather than fanning out, so the failure
  mode is a queue, not a storm — but watch the first two runs after merge.
- **I did not run the test suite**, per your standing rule that tests are CI-only.

**So: open a PR rather than merging directly if you want the suite to run first.** `ci.yml`
carries `pull_request:`, so a PR runs the tests against these changes; a direct merge to `main`
does not. That is the one thing that would catch a rename I missed.

---

## 5. If it goes wrong

- **Site still stale after a push:** check the `site / build` job's `head_sha` against the commit
  the `pipeline` job pushed. If they differ, the `ref` input did not take.
- **Pipeline retriggers itself:** remove `push:` from `pipeline.yml`. Everything else on this
  branch is independent of it.
- **Batch missing from the page:** the page reads `reports/wikidata-garborg-day.txt`. If the
  rename half-landed, that path is the thing to check first.
- **Revert cleanly:** every change is one of the six commits above; none depends on another
  except that (1) is what makes (2) worth having.

---

## 6. Open decisions that are yours, not mine

- **`daily-batch-email.yml`.** It rebuilds nothing, it is what sent you the stale batch under
  today's date, and the pipeline's own issue now does everything it did. Under
  `CLAUDE.md` § *LEGACY CODE IS DELETED* it should probably go. I did not delete a workflow of
  yours without being asked.
- **The `Den` lines stay.** Confirmed: they are your 2026-09-01 exception, they are the
  deduplication mechanism, and they are present in the batch at lines 24, 42, 58. I twice
  reported them to you as defects. Do not let a future session "fix" them — that is why the
  exception is now in `CLAUDE.md` rather than only in a code comment.
- **`reports/garborg-carry-forward.tsv` is not sorted by any obvious key.** With the pipeline now
  running on every push, an unsorted generated file churns on every run. Unaudited; your sorting
  rule says it should have a total key.
