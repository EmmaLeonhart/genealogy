# The links that block the paths

**`queue.md` § THE AGENDA asked which absent PERSON blocks the most paths.** That question no longer has an answer: every path member is present, because `exports/0-scraped/scraped-paths.ged` and `scraped-pages.ged` were built from these paths and ingested. What is missing is **edges**.

- **0 of 979 paths** hold a step the tree does not carry.
- **0 distinct missing links** cause it.

**A sibling step is carried through the shared parent** and is not counted as a break — `census-paths.connected` is the definition, reused here. Scored without it, all 2,126 sibling steps read as broken and the headline nearly doubles.

| paths blocked | from | to |
| ---: | --- | --- |
